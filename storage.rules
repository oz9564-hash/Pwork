rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 본인(uid) 폴더의 PDF·폰트 파일에만 읽기/쓰기 허용
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
